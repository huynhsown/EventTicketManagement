package com.ute.ticket.event.domain.enums;

public enum InventoryTransactionType {
    INITIAL,
    INCREASE,
    DECREASE,
    ADJUSTMENT,
    CORRECTION,
    RESET,
    SYNC,
    IMPORT,
    RESERVE,
    RELEASE,
    CONFIRM,
    EXPIRY,
    SYSTEM
}
